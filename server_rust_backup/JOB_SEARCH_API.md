# API для системы автоматического поиска работы

## Обзор

Система автоматически ищет вакансии на HH.ru, генерирует сопроводительные письма с помощью AI (OpenRouter), откликается на вакансии и мониторит чаты с рекрутерами.

## Настройка

### 1. Переменные окружения (.env)

```env
# OpenRouter API для генерации сопроводительных писем и ответов
OPENROUTER_API_KEY=your_openrouter_api_key
AI_MODEL=google/gemini-2.5-flash-lite

# HH.ru OAuth приложение
HH_CLIENT_ID=your_hh_client_id
HH_CLIENT_SECRET=your_hh_client_secret
HH_REDIRECT_URI=https://bgalin.ru/auth/hh/callback

# Интервал поиска новых вакансий (в часах)
JOB_SEARCH_INTERVAL_HOURS=4
```

### 2. Предварительные требования

Перед запуском автопоиска необходимо:
1. Добавить информацию "Обо мне" в портфолио (через `/api/portfolio/about`)
2. Добавить опыт работы (через `/api/portfolio/experience`)
3. Добавить навыки (через `/api/portfolio/skills`)
4. Добавить контакты: telegram и email (через `/api/portfolio/contacts`)
5. Авторизоваться через HH.ru OAuth

## Авторизация HH.ru

### 1. Начать OAuth процесс

```bash
GET /api/jobs/auth/hh
Authorization: Bearer <your_admin_token>
```

**Ответ:** URL для авторизации на HH.ru

Пример:
```bash
curl http://localhost:8000/api/jobs/auth/hh \
  -H "Authorization: Bearer your_token"
```

Откройте полученный URL в браузере и авторизуйтесь. После успешной авторизации вы будете перенаправлены на callback URL, и токен будет сохранен в БД.

## Настройка поиска

### Обновить параметры поиска

```bash
PUT /api/jobs/search/settings
Authorization: Bearer <your_admin_token>
Content-Type: application/json

{
  "search_text": "rust backend developer",
  "area_ids": ["1", "2"],  // 1 = Москва, 2 = Санкт-Петербург
  "experience": "between3And6",  // noExperience, between1And3, between3And6, moreThan6
  "schedule": "remote",  // fullDay, shift, flexible, remote, flyInFlyOut
  "employment": "full",  // full, part, project, volunteer, probation
  "salary_from": 200000,
  "only_with_salary": true
}
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "id": 1,
    "is_active": false,
    "search_text": "rust backend developer",
    "area_ids": "[\"1\",\"2\"]",
    "experience": "between3And6",
    "schedule": "remote",
    "employment": "full",
    "salary_from": 200000,
    "only_with_salary": true,
    "updated_at": "2025-11-05T18:30:00Z"
  }
}
```

## Управление автопоиском

### Запустить автопоиск

```bash
POST /api/jobs/search/start
Authorization: Bearer <your_admin_token>
```

**Что происходит:**
- Немедленно запускается первый поиск вакансий
- Система каждые N часов (из `JOB_SEARCH_INTERVAL_HOURS`) ищет новые вакансии
- Каждые 10 минут проверяет статусы откликов
- Каждые 10 минут мониторит чаты и отвечает на сообщения ботов

**Ответ:**
```json
{
  "success": true,
  "data": "Job search started"
}
```

### Остановить автопоиск

```bash
POST /api/jobs/search/stop
Authorization: Bearer <your_admin_token>
```

**Ответ:**
```json
{
  "success": true,
  "data": "Job search stopped"
}
```

### Проверить статус

```bash
GET /api/jobs/search/status
Authorization: Bearer <your_admin_token>
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "is_active": true,
    "is_authorized": true,
    "last_search": "2025-11-05T18:30:00Z",
    "settings": {
      "id": 1,
      "is_active": true,
      "search_text": "rust backend developer",
      "area_ids": "[\"1\",\"2\"]",
      "experience": "between3And6",
      "salary_from": 200000,
      "only_with_salary": true,
      "updated_at": "2025-11-05T18:30:00Z"
    }
  }
}
```

## Просмотр вакансий и откликов

### Получить все вакансии

```bash
GET /api/jobs/vacancies
Authorization: Bearer <your_admin_token>
```

**Ответ:**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "hh_vacancy_id": "12345678",
      "title": "Senior Rust Developer",
      "company": "Tech Company",
      "salary_from": 250000,
      "salary_to": 350000,
      "salary_currency": "RUR",
      "description": "...",
      "url": "https://hh.ru/vacancy/12345678",
      "status": "applied",
      "found_at": "2025-11-05T18:00:00Z",
      "applied_at": "2025-11-05T18:01:00Z",
      "updated_at": "2025-11-05T18:01:00Z",
      "response": {
        "id": 1,
        "vacancy_id": 1,
        "hh_negotiation_id": "abc123",
        "cover_letter": "Здравствуйте! Меня заинтересовала ваша вакансия...",
        "status": "sent",
        "created_at": "2025-11-05T18:01:00Z",
        "updated_at": "2025-11-05T18:01:00Z"
      }
    }
  ]
}
```

### Получить детали конкретной вакансии

```bash
GET /api/jobs/vacancies/<id>
Authorization: Bearer <your_admin_token>
```

### Получить вакансии по статусу

```bash
GET /api/jobs/vacancies/status/<status>
Authorization: Bearer <your_admin_token>
```

**Статусы:**
- `found` - вакансия найдена, но отклик еще не отправлен
- `applied` - отклик отправлен
- `viewed` - работодатель просмотрел отклик
- `invited` - приглашение на собеседование ⭐
- `rejected` - отказ
- `ignored` - вакансия проигнорирована вручную

Примеры:
```bash
# Получить приглашения на собеседование
GET /api/jobs/vacancies/status/invited

# Получить отказы
GET /api/jobs/vacancies/status/rejected

# Получить вакансии в процессе
GET /api/jobs/vacancies/status/applied
```

### Статистика откликов

```bash
GET /api/jobs/stats
Authorization: Bearer <your_admin_token>
```

**Ответ:**
```json
{
  "success": true,
  "data": {
    "total_found": 50,
    "total_applied": 45,
    "invited": 3,
    "rejected": 10,
    "in_progress": 32
  }
}
```

### Игнорировать вакансию

Если хотите вручную пометить вакансию как игнорируемую:

```bash
POST /api/jobs/vacancies/<id>/ignore
Authorization: Bearer <your_admin_token>
```

## Как работает система

### 1. Автоматический поиск (каждые N часов)

1. Система получает настройки поиска из БД
2. Запрашивает вакансии у HH.ru API по заданным критериям
3. Для каждой **новой** вакансии:
   - Сохраняет в БД со статусом `found`
   - Получает полное описание вакансии
   - Формирует резюме из портфолио (обо мне + опыт + навыки)
   - Генерирует сопроводительное письмо через OpenRouter AI
   - Отправляет отклик на вакансию через HH.ru API
   - Обновляет статус на `applied`
   - Генерирует краткое вступительное сообщение для чата
   - Отправляет вступительное сообщение в чат вакансии

### 2. Мониторинг откликов (каждые 10 минут)

1. Получает все отклики с HH.ru API
2. Обновляет статусы вакансий:
   - `invitation` → `invited` (приглашение на собеседование)
   - `discard` → `rejected` (отказ)
   - `response` → `viewed` (просмотрено)

### 3. Мониторинг чатов (каждые 10 минут)

1. Получает все вакансии со статусами `applied`, `viewed`, `invited`
2. Для каждой вакансии проверяет наличие новых сообщений в чате
3. Если есть новое сообщение от работодателя:
   - Определяет, бот это или человек (по ключевым словам)
   - Если бот с вопросами - автоматически генерирует ответ через AI
   - Отправляет ответ в чат
   - Сохраняет информацию о чате в БД

### 4. Генерация сопроводительных писем

AI получает:
- Название вакансии
- Описание вакансии
- Ваше резюме (из портфолио)
- Ваши контакты (telegram, email)

И генерирует профессиональное сопроводительное письмо на русском языке (3-4 абзаца) с контактами в конце:
```
Контакты для связи:
Telegram: https://t.me/username
Email: email@example.com
Портфолио: https://bgalin.ru/resume
```

### 5. Ответы на вопросы ботов

Если система обнаруживает сообщение от бота (по паттернам типа "пройдите тест", "ответьте на вопросы"), она:
1. Отправляет вопрос AI вместе с вашим резюме и названием вакансии
2. Получает профессиональный ответ
3. Автоматически отправляет его в чат

## Типичный сценарий использования

```bash
# 1. Авторизация через HH.ru
curl http://localhost:8000/api/jobs/auth/hh \
  -H "Authorization: Bearer your_token"
# Откройте URL в браузере и авторизуйтесь

# 2. Настройка параметров поиска
curl -X PUT http://localhost:8000/api/jobs/search/settings \
  -H "Authorization: Bearer your_token" \
  -H "Content-Type: application/json" \
  -d '{
    "search_text": "rust backend developer",
    "area_ids": ["1"],
    "salary_from": 200000,
    "experience": "between3And6"
  }'

# 3. Запуск автопоиска
curl -X POST http://localhost:8000/api/jobs/search/start \
  -H "Authorization: Bearer your_token"

# 4. Проверка статуса через некоторое время
curl http://localhost:8000/api/jobs/search/status \
  -H "Authorization: Bearer your_token"

# 5. Просмотр статистики
curl http://localhost:8000/api/jobs/stats \
  -H "Authorization: Bearer your_token"

# 6. Просмотр приглашений на собеседование
curl http://localhost:8000/api/jobs/vacancies/status/invited \
  -H "Authorization: Bearer your_token"

# 7. Просмотр всех вакансий
curl http://localhost:8000/api/jobs/vacancies \
  -H "Authorization: Bearer your_token"
```

## Логи

Система выводит информативные логи в консоль:

```
🔄 Job scheduler background task started
🔍 Running initial job search...
📋 Found 25 vacancies
✅ Applied to: Senior Rust Developer
✅ Applied to: Backend Developer (Rust)
✅ Job search completed. Applied to 3 new vacancies
💬 New message in chat for: Senior Rust Developer
🤖 Detected bot message, generating response...
✅ Sent auto-response to bot
```

## Примечания

- Rate limiting: между откликами - 5 секунд, между проверками чатов - 2 секунды
- Система не откликается дважды на одну и ту же вакансию
- Сопроводительные письма сохраняются в БД и доступны через API
- Чаты мониторятся только для вакансий со статусами `applied`, `viewed`, `invited`
- Автоответы отправляются только на сообщения от ботов, не от людей
- При остановке автопоиска все процессы мониторинга также останавливаются

## Коды городов (area_ids)

- `1` - Москва
- `2` - Санкт-Петербург
- `3` - Екатеринбург
- `4` - Новосибирск
- `88` - Казань
- `113` - Россия (все города)

Полный список: https://api.hh.ru/areas
